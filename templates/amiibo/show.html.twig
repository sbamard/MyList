{% extends 'base.html.twig' %}

{% block title %}Amiibo{% endblock %}

{% block body %}
    <h1>Amiibo</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ amiibo.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ amiibo.name }}</td>
            </tr>
            <tr>
                <th>Serie</th>
                <td>{{ amiibo.serie }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('amiibo_index') }}">back to list</a>

    <a href="{{ path('amiibo_edit', {'id': amiibo.id}) }}">edit</a>

    {{ include('amiibo/_delete_form.html.twig') }}
{% endblock %}
